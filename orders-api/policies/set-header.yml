kind: Set
apiVersion: api.ibm.com/v1
metadata:
  name: set-header
  namespace: orders-api
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  valueType: jsonata
  messageHeader:
    messageName: response
    value: application/json
